class Solution:
    def isPalindrome(self, x: int) -> bool:
        if x<0:
            return False
        y=str(x)
        rev=y[::-1]
        if rev==y:
            return True
        else:
            return False
