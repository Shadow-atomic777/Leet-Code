class Solution:
    def isPalindrome(self, s: str) -> bool:
        
        str="".join([char for char in s if char.isalpha() or char.isdigit()])
        str=str.lower()
        if str[::]==str[::-1]:
            return True
        else:
            return False
